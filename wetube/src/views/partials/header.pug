header
    a(href="/").header__logo
      i.fab.fa-youtube
    nav 
      ul
        li
          a(href="/search")
            i.fas.fa-search
        if !loggedIn
          li
            a(href="/login") Login 
          li
            a(href="/join").header__btn Join
        else 
          li 
            a(href='/users/logout') Logout
          li 
            a(href=`/users/${loggedInUser._id}`) My Profile
          li 
            a(href='/users/mypage') Edit #{loggedInUser.name}'s Profile 
          li
            a(href="/videos/upload") Upload Video