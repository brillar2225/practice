header
    a(href="/").header__logo
      i.fab.fa-youtube
    nav 
      ul
        li
          a(href="/search")
            i.fas.fa-search
        if !loggedIn
          li
            a(href="/login") Login 
          li
            a(href="/join").header__btn Join
        else 
          li 
            a(href='/users/logout') Logout
          li
            a(href="/videos/upload") Upload Video
          li 
            a(href=`/users/${loggedInUser._id}`)
              if !loggedInUser.avatarUrl
                span 😊
              else
                img(src=loggedInUser.socialOnly === false ? '/' + loggedInUser.avatarUrl : loggedInUser.avatarUrl, alt="profile image").header__avatar